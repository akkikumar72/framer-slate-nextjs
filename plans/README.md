# Implementation Plans

Generated by the improve skill on 2026-08-10 at commit `e5c073c`. The user
selected all five findings and explicitly requested one pull request. Execute
the plans below in order in this branch. The executor must read every plan
fully, honor each STOP condition, and leave the index updates to the reviewer.

## Execution order and status

| Plan | Title | Priority | Effort | Depends on | Status |
|---|---|---|---|---|---|
| 001 | Restore reproducible installs and the 13-template catalog | P1 | S | none | DONE |
| 002 | Make route contracts prove route identity | P1 | L | 001 | DONE |
| 003 | Add serial Chromium interaction coverage for every app | P1 | L | 001, 002 | DONE |
| 004 | Make all preview forms truthful | P1 | M | 003 | DONE |
| 005 | Cut Dashfluence homepage service-image weight | P2 | M | 003 | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale)

## Dependency notes

- 001 must run first because the current lockfile prevents `npm ci`, and the
  showcase's stale route marker would fail the existing route suite next.
- 002 runs before browser coverage so the browser suite consumes complete,
  truthful route identity contracts.
- 003 provides the real-browser safety net required by the user-facing form
  and image changes in 004 and 005.
- 004 and 005 are logically independent after 003. They remain sequential here
  because the user requested one executor branch and one pull request.

## One-PR boundary

The single pull request may contain only the files named by plans 001-005 plus
these plan files and reviewer evidence. Do not add linting, Turbo caching,
server-component refactors, a form backend, unrelated copy cleanup, or general
asset deduplication. Every changed line must trace to one selected finding.

## Findings considered and rejected for this pull request

- A real form-delivery backend was rejected. It would introduce PII handling,
  consent, abuse prevention, secrets, retries, and operational ownership that
  the user did not select. Preview forms must be explicit about not sending.
- A global Playwright `webServer` array was rejected because it would start all
  14 applications together. The chosen harness runs one production server at
  a time.
- A generic interaction DSL inside every `template.config.json` was rejected.
  Fourteen unrelated UI states are clearer as a guarded slug-to-handler map.
- Recompressing Dashfluence's detail heroes was rejected. The two originals
  remain for full-width detail pages; homepage-only derivatives solve the
  selected transfer problem with lower visual risk.
- Repository-wide client-boundary, lint, formatting, Turbo-cache, licensing,
  and shared asset-audit work remains useful but is outside this selected PR.
- Cross-template visual component sharing was rejected because each app has an
  independent design and deployment boundary.
