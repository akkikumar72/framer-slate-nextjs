# Rivero

Standalone Next.js rebuild of the Rivero HR management template.

```bash
npm run dev --workspace=@framer-templates/rivero
```

The app runs at [http://localhost:3011/](http://localhost:3011/).
